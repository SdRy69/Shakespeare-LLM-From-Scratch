# Direct runtime dependencies for Shakespeare-LLM-From-Scratch.
# Python 3.10+ recommended. Install: pip install -r requirements.txt
#
# Standard library (do not pip-install): os, time, urllib, typing.

torch>=2.0.0
numpy>=1.24.0
